import Form from 'components/Form/Form';

const Header = () => {
  return (
    <div>
      <button>
        Create Room
      </button>
      <Form
        submitFn={() => {

        }}
        inputItems={[
          
        ]}
      />
    </div>
  );
};

export default Header;
